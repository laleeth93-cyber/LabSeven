const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const settings = await prisma.reportSettings.findFirst(); console.log(settings.customHeader1?.substring(0, 20), settings.customHeader2?.substring(0, 20), settings.customHeader3?.substring(0, 20), settings.customHeader4?.substring(0, 20)); } main().finally(() => prisma.$disconnect());
